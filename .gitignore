# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Bot registry (mapeamento LID → número, dados de runtime)
.bot-registry.json

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Environment & Secrets (AIOX)
.env
.env.local
.env.*.local
*.key

# Dependencies (AIOX)
node_modules/

# Build & Logs (AIOX)
dist/
build/
*.log
logs/

# IDE & OS (AIOX)
Thumbs.db
.idea/
*.swp

# AIOX Local (AIOX)
.aiox-core/
.aiox-core/local/
.claude/settings.local.json
.aiox/install-log.txt

# Local tooling
.codex/
.claude/
.cursor/
.gemini/
.runtime/
.antigravity/

# WhatsApp bridge — sessões e credenciais (NUNCA versionar)
08 - IA COMUNICACAO/bridge/auth_session/
08 - IA COMUNICACAO/bridge/auth_session_*/
08 - IA COMUNICACAO/bridge/pino.log

# Docker Compose com credenciais reais (usar .example como referência)
docker-compose.evolution.yml

# Arquivos temporários de office
.~lock.*
*.tmp

# Arquivos de apresentação/PDF grandes (binários locais)
*.pptx
*.pdf
